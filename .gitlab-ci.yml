stages:
  - build
  - test

variables:
  npm_config_cache: "/builds/santiago105/lumina-testing/.npm"
  CYPRESS_CACHE_FOLDER: "/builds/santiago105/lumina-testing/cache/Cypress"
  SLACK_WEBHOOK: "https://hooks.slack.com/services/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"

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - .npm
    - cache/Cypress
    - node_modules

install:
  image: cypress/base:latest
  stage: build

  script:
    # create necessary folders if they don't exist
    - if [ ! -d "$npm_config_cache" ]; then mkdir "$npm_config_cache"; fi
    - if [ ! -d "$CYPRESS_CACHE_FOLDER" ]; then mkdir "$CYPRESS_CACHE_FOLDER"; fi
    # check Cypress binary path and cached versions
    - apt-get update
    - apt-get install -y default-jdk
    - export JAVA_HOME=$(/usr/libexec/java_home)
    - echo "JAVA_HOME=$JAVA_HOME"
    - npx cypress cache path
    - npx cypress cache list
    - $(npm bin)/print-env CI
    - npx cypress info
    - npx cypress verify

run_tests:
  image: cypress/base:latest
  stage: test
  script:
    - apt-get update
    - apt-get install -y openjdk-11-jdk
    - sudo apt-get install chromium-chromedriver
    - chromedriver
    - npm ci
    - export JAVA_HOME=$(/usr/libexec/java_home)
    - echo $JAVA_HOME
    - java -version
    - /usr/libexec/java_home
    - $(npm bin)/print-env CI
    - rm -r allure-results/ allure-report cypress/screenshots || true
    - allure generate mochawesome-report/mochawesome-report.json --output allure-results
    - mv -f allure-report/history allure-results/history && rm -r allure-report || true
  after_script:
  - echo "Sending Slack Message with report link"
  - report_link="https://your-gitlab-domain.com$CI_JOB_URL/artifacts/allure-report"
  - echo report_link
  - "curl -X POST -H 'Content-type: application/json' --data '{\"text\":\"Allure report is ready! Check it out: https://your-gitlab-domain.com$CI_JOB_URL/artifacts/allure-report\"}' https://hooks.slack.com/services/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"
  artifacts:
    paths:
      - allure-report
      - allure-results/history
      - cypress/videos
      - cypress/screenshots
    expire_in: 1 day